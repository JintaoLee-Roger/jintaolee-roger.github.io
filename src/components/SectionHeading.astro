---
import T from "./T.astro";

const { eyebrow, title, intro } = Astro.props;
---

<div class="section-heading">
  {eyebrow && <p class="eyebrow"><T value={eyebrow} /></p>}
  <h2><T value={title} /></h2>
  {intro && <p class="section-intro"><T value={intro} /></p>}
</div>
